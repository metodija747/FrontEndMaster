describe('Lesa Ljubljana Website', () => {
// register, login(response), catalog(addToCart na first), product details(img) na vtor, add comment(response), delete comment(response),
//  addToCart(response), openCart(broj na elementi da e 2), updateQuantity(response), deleteAllFromCart(broj na elementi 0)
// closeCart and deleteUser
  beforeEach(() => {
    // Visiting the website before each test
    cy.visit('http://metodija747.s3-website-us-east-1.amazonaws.com/');
  });

})
